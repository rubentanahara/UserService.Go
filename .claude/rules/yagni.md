# YAGNI Guardrails

- No interface with one implementation, no factory for one product, no config for a value that never changes.
- Skip caching layers, generic base services, event buses until a real need (profiler, actual duplication) shows up.
