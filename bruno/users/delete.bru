meta {
  name: Delete User
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("returns 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
